# ──────────────────────────────────────────────────────────────────────────────
# Copy this file to .env and fill in real values before running the dev server
# or building for production.
# NEVER commit the real .env file to version control.
# All variables exposed to the browser MUST be prefixed with VITE_.
# ──────────────────────────────────────────────────────────────────────────────

# Backend API base URL (used by src/lib/api.js)
# In development the Vite proxy forwards /api → http://localhost:8000,
# so this can stay as-is. Override for staging/production deployments.
VITE_API_BASE_URL=http://localhost:8000/api
